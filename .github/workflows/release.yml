name: Trigger release

# This workflow creates a new release for the library.
# It can be triggered manually from the GitHub Actions UI.
#
# The workflow performs the following steps:
# 1. Checks out the latest commit from the main branch
# 2. Reads the current version from gradle.properties
# 3. Creates a git tag and GitHub release with auto-generated release notes
# 4. Calculates the next version (increments minor version, resets patch to 0)
# 5. Updates gradle.properties with the new version
# 6. Commits and pushes the version update back to the main branch
#
# Example: If current version is 2.16.0, this workflow will:
# - Create tag v2.16.0 and release from the current main branch commit
# - Update gradle.properties to version 2.17.0
# - Commit and push the version update to main

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: adopt
          java-version: 17

      - name: Get current version
        id: get_version
        run: |
          CURRENT_VERSION=$(grep "^libVersion=" gradle.properties | cut -d'=' -f2)
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "Current version: $CURRENT_VERSION"

      - name: Create tag and release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VERSION: ${{ steps.get_version.outputs.current_version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          TAG_NAME="v${VERSION}"
          echo "Creating tag: $TAG_NAME"
          
          # Create and push tag
          git tag -a "$TAG_NAME" -m "Release $TAG_NAME"
          git push origin "$TAG_NAME"
          
          # Create release with auto-generated notes
          gh release create "$TAG_NAME" \
            --title "Release $TAG_NAME" \
            --generate-notes \
            --target main

      - name: Calculate next version
        id: next_version
        run: |
          CURRENT_VERSION=$(grep "^libVersion=" gradle.properties | cut -d'=' -f2)
          echo "Current version: $CURRENT_VERSION"
          
          # Parse semantic version (major.minor.patch)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"
          
          # Increment minor version and reset patch to 0
          NEXT_MINOR=$((MINOR + 1))
          NEXT_VERSION="${MAJOR}.${NEXT_MINOR}.0"
          
          echo "next_version=$NEXT_VERSION" >> $GITHUB_OUTPUT
          echo "Next version: $NEXT_VERSION"

      - name: Update version in gradle.properties
        run: |
          NEXT_VERSION="${{ steps.next_version.outputs.next_version }}"
          echo "Updating gradle.properties to version: $NEXT_VERSION"
          sed -i "s/^libVersion=.*/libVersion=$NEXT_VERSION/" gradle.properties
          
          # Verify the change
          echo "Updated version:"
          grep "^libVersion=" gradle.properties

      - name: Commit and push version update
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          NEXT_VERSION="${{ steps.next_version.outputs.next_version }}"
          
          git add gradle.properties
          git commit -m "Bump version to $NEXT_VERSION"
          git push origin main
